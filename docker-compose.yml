version: '3.7'

services:
  apm-elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:7.7.1
    container_name: apm-elasticsearch
    restart: unless-stopped
    network_mode: bridge
    ports:
      - 9200:9200
    volumes:
      - ./elasticsearch_data:/usr/share/elasticsearch/data
      - ./elasticsearch.yml:/usr/share/elasticsearch/config/elasticsearch.yml
      - ./certs:/usr/share/elasticsearch/config/certificates
    environment:
      - ES_JAVA_OPTS=-Xms512m -Xmx512m
    logging:
        driver: "json-file"
        options:
            max-file: "5"
            max-size: "10m"

  apm-kibana:
    image: docker.elastic.co/kibana/kibana:7.7.1
    container_name: apm-kibana
    restart: unless-stopped
    network_mode: bridge
    environment:
      - SERVER_HOST=0.0.0.0
    volumes:
      - ./kibana.yml:/usr/share/kibana/config/kibana.yml
      - ./certs:/usr/share/kibana/config/certs
    ports:
      - 50601:5601
    links:
      - apm-elasticsearch:elasticsearch
    depends_on:
      - apm-elasticsearch
    logging:
        driver: "json-file"
        options:
            max-file: "5"
            max-size: "10m"

  apm-server:
    image: docker.elastic.co/apm/apm-server:7.7.1
    container_name: apm-server
    restart: unless-stopped
    network_mode: bridge
    ports:
      - 8200:8200
    command: [
         "apm-server",
         "-e",
         "-E", "output.elasticsearch.username=elastic",
         "-E", "output.elasticsearch.password=7Xi4QNfIhfINUQNWSDty",
         "-E", "apm-server.kibana.username=apm_system",
         "-E", "apm-server.kibana.password=K3AD9BvLKvXY8x2cu71v"
    ]
    environment:
      - apm-server.host="0.0.0.0:8200"
      - apm-server.secret_token="qV2qwpXCVDYzkqCBWybTEDVcj9h5Qazm2v643U5sZNXVqwgB6An4ThUu5r64"
      - setup.template.enabled=true
      - logging.to_files=false
    volumes:
      - ./apm-server.yml:/usr/share/apm-server/apm-server.yml
      - ./certs:/usr/share/apm-server/certs
    links:
      - apm-elasticsearch:elasticsearch
      - apm-kibana:kibana
    depends_on:
      - apm-elasticsearch
      - apm-kibana
    logging:
        driver: "json-file"
        options:
            max-file: "5"
            max-size: "10m"