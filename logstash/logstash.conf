input {
	beats {
		port => 5044
	}

	tcp {
		port => 5000
	}
}

output {
	elasticsearch {
		hosts => "elasticsearch:9200"
		user => "elastic"
		password => "changeme",
		ssl => true
        cacert => '/etc/logstash/certs/ca/ca.crt'
		ecs_compatibility => disabled
	}
}